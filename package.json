  {
    "name": "armoredcode", 
    "private": true,
    "workspaces": [
      "client",
      "api"
    ],

    "dependencies": {
      "@faker-js/faker": "^8.4.1",
      "mongodb": "^6.6.1",
      "nodemailer": "^6.9.13"
    },
      "scripts": {
        "start": "yarn workspace api start",
        "dev:client": "yarn workspace client start",
        "build": "yarn workspace client build && yarn workspace api build"
      }

  }
